import { FilterListingPipe } from './filter-listing.pipe';

describe('FilterListingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListingPipe();
    expect(pipe).toBeTruthy();
  });
});
