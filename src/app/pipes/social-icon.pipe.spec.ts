import { SocialIconPipe } from './social-icon.pipe';

describe('SocialIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialIconPipe();
    expect(pipe).toBeTruthy();
  });
});
